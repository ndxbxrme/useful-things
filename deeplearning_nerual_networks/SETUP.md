ubuntu 16.04  
install notes 

```
    6  sudo apt-get install python-pip
    7  sudo apt-get install python-dev
    9  sudo apt install git
   12  sudo apt-get install libblas-dev liblapack-dev libatlas-base-dev gfortran
   18  sudo apt-get install libhdf5-dev cython
   42  sudo apt-get install gdebi
   57  sudo apt update
   59  sudo apt install libqtcore4 libqtgui4
   64  sudo apt install cmake
   66  sudo apt-get install -y build-essential gcc g++ curl cmake libreadline-dev git-core 
        libqt4-dev libjpeg-dev libpng-dev ncurses-dev imagemagick libzmq3-dev gfortran  
        unzip gnuplot gnuplot-x11 ipython
   75  sudo apt-get update
   76  sudo apt-get install cuda
   77  sudo apt install nvidia-cuda-toolkit
  176  history | grep "apt-get install"
  185  history | grep "apt"
```

```
    6  sudo apt-get install python-pip
   13  sudo pip install git+https://github.com/Theano/Theano.git
   14  pip install --upgrade pip
   15  sudo pip install git+https://github.com/basveeling/keras.git@wavenet#egg=keras
   16  sudo pip install picklable_itertools
   17  sudo pip install sacred
   19  sudo pip install h5py
   20  sudo pip install tqdm
   21  sudo pip install q
   56  sudo pip install libqt4-core libqt4-gui
```
